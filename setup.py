
from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='to decrease bounce rate in medical inventory',
    author='yogesh_thakur',
    license='MIT',
)

from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='to decrease bounce rate in medical inventory',
    author='yogesh_thakur',
    license='MIT',
)
